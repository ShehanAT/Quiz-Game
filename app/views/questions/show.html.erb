<div>
<h3 id="question_name"><i class="heading_text">Question:</i> <%= @question[0]["question"] %></h3>
<h3 id="quiz_name"><i class="heading_text">Belongs to Quiz Name:</i> <%= @question[0]["name"] %></h3>
<% @answers.each_with_index do |answer, index| %>
    <%= content_tag(:h3, "<i class='heading_text'>Answer Choice #{index + 1}:</i>  #{answer["answer"]}".html_safe, :id => "answer#{index+1}", :value => "answer#{index+1}") %>
<% end %>
<% @answers.each_with_index do |answer, index| %>
    <% if answer["correct_answer"] == 1 %>
        <%= content_tag(:h3, "<i class='heading_text'>Correct Answer:</i> #{answer["answer"]}".html_safe, :id => "correct_answer") %>
    <% end %>
<% end %>
<% if @question[0]["contains_images"] === 1 %>
    <h3><i class="heading_text">Question Type:</i> Image Question</h3>
    <img src="<%= @question_image.image_url %>" alt="About Question" />
<% elsif @question[0]["contains_images"] === 0 %>
    <h3><i class="heading_text">Question Type:</i> Text Question</h3>
<% end %>
</div>
<div class="btn_container">
    <%= button_to "Edit This Question", edit_question_path(@question[0]["id"]), id: "edit_question_link", :class => "btn btn-primary", method: :get, :question_id => @question[0]["id"] %>
    <button class="btn btn-danger" onclick="showModal()" id="delete_question_link" >Delete Question</button>
    <%= button_to "Back", questions_path, :class => "btn btn-dark back", method: :get  %>
</div>
<%= render partial: "questions/html/delete_question_modal" %> 
<script>
    <%= render "questions/js/delete_question.js.erb" %>
</script>