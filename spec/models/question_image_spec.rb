require 'rails_helper'

RSpec.describe QuestionImage, type: :model do

  it "questionImage instance should be valid " do 
    question_image = QuestionImage.new 
  end   
  # it "questionImage instance should store sample file in GCS" do 
  #   question_image = QuestionImage.new 
  #   //question_image.upload_image
  #   expect(question_image).to be_an_instance_of(QuestionImage)
  # end 





end
